# [기술 문서] Kocom RS485 패킷 안전성 강화 (Passive Query) 및 로깅 고도화 (v2.4.2)

이 문서는 v2.4.2 버전에서 적용된 **전면적인 패시브 조회(Full Passive Query)** 정책과 **패킷 해석 로깅(Interpreted Logging)** 시스템에 대해 기술합니다.

---

### 1. 배경: Active Query의 위험성

이전 버전(v2.3.x ~ v2.4.1)까지 일부 기기(에어컨, 환기, 난방 등)의 상태 조회(`Query`) 로직은 "HA가 기억하고 있는 마지막 상태"를 패킷에 담아 보내는 **Active Query** 방식을 사용했습니다.

*   **의도:** 상태 조회와 동시에 기기 상태를 동기화하려는 목적.
*   **부작용 (Incident):** 
    1.  HA가 초기화되거나 잘못된 상태값(예: 온도 40도, 팬 속도 0 등)을 가지고 있을 때, 이를 그대로 기기에 전송함.
    2.  일부 민감한 월패드/기기는 이를 **"설정 변경 명령"**으로 오인하여 즉시 동작함.
    3.  **결과:** 
        *   새벽에 보일러 설정 온도가 40도로 자동 변경됨 (Thermostat Incident).
        *   가스 밸브 제어기가 주기적으로 전원이 차단됨 (Gas Valve Power Cut).
        *   네트워크 재연결 시 조명이 일제히 켜짐 (Ghost Light).

---

### 2. 해결책: Full Passive Query 도입

v2.4.2부터는 모든 제어 가능한 기기(Switch, Light, Thermostat, AC, Ventilation, Gas)에 대해 **Passive Query** 정책을 강제합니다.

#### **설계 원칙**
> **"Query 패킷은 질문이어야 하지, 명령이어야 해서는 안 된다."**

*   **변경 전 (Active):** `[Header] ... [Command=0x00] [Data=TargetTemp/FanSpeed/OnFlag...]`
*   **변경 후 (Passive):** `[Header] ... [Command=0x00] [Data=00 00 00 00 00 00 00 00]`

#### **적용 범위**
1.  **Thermostat (난방):** 조회 시 `Data`를 모두 `0x00`으로 전송. (40도 설정 사고 방지)
2.  **Air Conditioner (에어컨):** 조회 시 `Data`를 모두 `0x00`으로 전송. (불필요한 비프음 및 설정 변경 방지)
3.  **Ventilation (환기):** 조회 시 `Data`를 모두 `0x00`으로 전송.
4.  **Gas Valve (가스):** 
    *   조회 및 닫기 명령 시 `0x00` 커맨드 사용 (기존 `0x02` 하드코딩 제거).
    *   `0x02` 커맨드가 유발하던 전원 차단/리셋 문제 해결.

---

### 3. 로깅 고도화 (Interpreted Logging)

문제가 발생했을 때 사용자가 원인을 직관적으로 파악할 수 있도록, 로그 출력 방식을 개선했습니다.

*   **기존:** `Packet sent: AA5530BC00360001000011001800000000004C0D0D` (해석 난해)
*   **개선:** `Gateway: 패킷 전송 -> Type=THERMOSTAT, Room=0, Cmd=0x00, Data=110018... (Mode=HEAT, Curr=24C, Set=24C)`

이제 로그만 보고도 "HA가 난방을 24도로 켜라고 명령했구나" 또는 "단순히 상태만 물어봤구나(Data=00...)"를 명확히 구분할 수 있습니다.

---

### 4. 결론

v2.4.2는 기능 추가보다는 **안전성(Safety)**과 **관측 가능성(Observability)**에 집중한 업데이트입니다. 특히 RS485 통신의 특성상 잘못된 패킷 하나가 물리적인 불편함(폭염, 정전 등)을 초래할 수 있음을 인지하고, 방어적인 통신 설계를 기본 원칙으로 재수립했습니다.
